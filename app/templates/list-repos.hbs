<ul>
    {{#each @model as |repo|}}
    <li>
        <LinkTo @route="repo-details" @models={{array repo.name repo.owner}} @query={{hash limit="5"}}>
            {{repo.name}}
        </LinkTo>
    </li>
    {{/each}}
</ul>